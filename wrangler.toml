# Cấu hình cho Cloudflare Workers — chỉ phục vụ trang tĩnh trong thư mục web/
# Dòng name phải trùng tên Worker trên Cloudflare (Settings -> General -> Name).
name = "dathang"
compatibility_date = "2025-09-01"

[assets]
directory = "./web"
